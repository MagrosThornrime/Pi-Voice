# Audio Platform

## Building

Installing libraries:
```bash
make setup
```

Building C++ to `.node` and pack Electron Application:
```bash
make
```

## Using tests

If on linux, use before:
```bash
sudo chmod u+x run_tests.bash
```

To use tests:

```bash
make tests
```